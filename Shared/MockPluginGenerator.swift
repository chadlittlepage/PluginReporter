import Foundation

/// Placeholder for the original `MockPluginGenerator` (file lost; no live
/// call sites in the current codebase). Kept as an empty enum so the Xcode
/// project's file reference still resolves. Re-implement when sample data
/// or preview fixtures are needed.
enum MockPluginGenerator {
}
